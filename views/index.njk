{% extends "layout.njk" %}
{% block  content %}
  <ul>
    {% for row in rows %}
      <a href="/post/{{ row.id }}">
        <li class="post">
          <div class="leftside">
            <h2>{{ row.title }}</h2>
            <b>Författare:</b> {{ row.username }}<br>
            <b>Tid:</b> {{ row.createdAt  }}<br>
            <b>Meddelande:</b> {{ row.content  }}<br>
          </div>
          <div class="line"></div>
          <div class="rightside">
            <h4>{{ row.id }}</h4>
          </div>
        </li>
      </a>
    {% endfor %}
  </ul>
{% endblock %}